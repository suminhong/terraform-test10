# 바로 시작하기

VPC 권한 있는 IAM User 생성해서 ACCESS KEY랑 SECRET KEY 등록 후에 진행

<pre><code>git clone https://github.com/suminhong/terraform-test10.git
cd terraform-test10/stage/
terraform init
terraform apply</code></pre>
